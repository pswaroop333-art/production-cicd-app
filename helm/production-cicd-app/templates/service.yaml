apiVersion: v1
kind: Service
metadata:
  name: production-cicd-app-service
  namespace: production

spec:
  type: {{ .Values.service.type }}

  selector:
    app: production-cicd-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
